[module/bluetooth]
type = custom/script
exec = ~/.config/polybar/scripts/bluetooth.sh --show
interval = 2
click-left = exec blueman-manager && exec killall blueman-tray
click-right = exec ~/.config/polybar/scripts/bluetooth.sh --toggle
format-background = ${color.background}
format-foreground = ${color.foreground}
